name: "API Tests"

before:
  run: echo "Starting API server on :8080"
  timeout: "5s"

after:
  run: echo "Stopping API server"
  timeout: "5s"

scenarios:
  - id: health
    name: "Health check"
    run:
      command: echo "GET /health -> 200 OK"
      timeout: "5s"
