name: "User API"

before_each:
  run: echo "Creating test user"
  timeout: "2s"

scenarios:
  - id: list
    name: "List users"
    run:
      command: echo "GET /users -> [{id:1,name:alice}]"
      timeout: "5s"

  - id: create
    name: "Create user"
    run:
      command: echo "POST /users -> 201 Created"
      timeout: "5s"
    assertions:
      - command: "true"
        timeout: "1s"
