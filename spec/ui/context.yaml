name: "UI Tests"

before:
  run: echo "Launching headless browser"
  timeout: "10s"

after:
  run: echo "Closing browser"
  timeout: "5s"

scenarios:
  - id: homepage
    name: "Homepage loads"
    run:
      command: echo "Navigating to / ... OK"
      timeout: "10s"

  - id: checkout
    name: "Checkout flow"
    run:
      command: echo "Add to cart -> Checkout -> Payment -> Done"
      timeout: "30s"
